# Required — same NeonDB URL for localhost AND Render deployment
DATABASE_URL=postgresql://user:password@your-host.neon.tech/neondb?sslmode=require

# Render: add DATABASE_URL in Dashboard → Environment (or upload as Secret File at /etc/secrets/.env)
DEBUG=false
SECRET_KEY=your-secret-key
